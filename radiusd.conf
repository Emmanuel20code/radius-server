# Basic configuration
# Bind only to localhost (127.0.0.1) to avoid 
# conflicts with the container's external network stack.
listen {
    type = auth
    ipaddr = 127.0.0.1
    port = 2083
}

listen {
    type = acct
    ipaddr = 127.0.0.1
    port = 2083
}
